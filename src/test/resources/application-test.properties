# Test yapılandırması

# Server
server.port=0

# Logging
logging.level.root=INFO
logging.level.io.mersel.dss.signer.api=DEBUG

# Test keystore (gerçek ortamda kullanılmamalı)
PFX_PATH=classpath:test-keystore.pfx
CERTIFICATE_PIN=test123
CERTIFICATE_ALIAS=
CERTIFICATE_SERIAL_NUMBER=

# Sertifika zinciri (test için boş)
CERTIFICATE_CHAIN_GET_ONLINE=false
ISSUER_CERTIFICATE_PATH=
CA_CERTIFICATE_PATH=

# Timestamp (test için devre dışı)
IS_TUBITAK_TSP=false
TS_SERVER_HOST=http://localhost:9999
TS_USER_ID=test
TS_USER_PASSWORD=test
TS_DIGEST_ALGORITHM=SHA-256

# CORS
cors.allowed-origins=*
cors.allowed-methods=GET,POST
cors.max-age=3600

# Max session count
MAX_SESSION_COUNT=5

# KamuSM
kamusm.root.url=http://localhost:9999

